/*global define, describe, beforeEach, it, expect */
define( [
	'Class',
	'data/persistence/writer/Writer'
], function( Class, Writer ) {
	
	describe( 'data.persistence.writer.Writer', function() {
		/*var ConcreteWriter = Class.extend( Writer, {
			
		} );*/
	} );
	
} );